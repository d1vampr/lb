#include <iostream>

using namespace std;

int main()
{
    cout  "A + B = "  a + b  '\n'
         "A - B = "  a - b  '\n'
         "A * B = "  a * b  '\n'
         "A / B = "  a / b  '\n'
        max_number = max(numbers)
        print("Максимум:", max_number)
        min_number  min(numbers)
        print("Минимум:", min_number)
}
